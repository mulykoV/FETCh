@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Підтвердження реєстрації";
}

<div class="hero-split register-container">
    <!-- Ліва частина з матричним ефектом -->
    <div class="hero-half dark-side">
        <canvas id="matrixRain"></canvas>
    </div>

    <!-- Права частина з контентом -->
    <div class="hero-half light-side">
        <div class="content-box">
            <h1 class="title">@ViewData["Title"]</h1>

            @if (Model.DisplayConfirmAccountLink)
            {
                <p class="info-text">
                    Цей застосунок зараз не має підключеного реального email-відправника.<br />
                    Подивись <a class="console-link" target="_blank" href="https://aka.ms/aspaccountconf">документацію</a>, як його налаштувати.
                </p>
                <p class="confirm-box">
                    Нормально тут би було посилання на email:<br />
                    <a id="confirm-link" class="console-btn" href="@Model.EmailConfirmationUrl">✅ Підтвердити акаунт</a>
                </p>
            }
            else
            {
                <p class="info-text">
                    Будь ласка, перевір свою поштову скриньку і підтвердь акаунт.
                </p>
            }
        </div>
    </div>
</div>
