@model FETChModels.Models.Module

@{
    ViewData["Title"] = "СТВОРЕННЯ_МОДУЛЯ";
}

<div class="cyber-container fade-in">
    <div class="row mb-4">
        <div class="col-12 text-center">
            <h1 class="glitch-text" data-text="НОВИЙ МОДУЛЬ">НОВИЙ МОДУЛЬ</h1>
            <p class="pixel-text text-muted">> РОЗШИРЕННЯ СТРУКТУРИ КУРСУ [LINKED_TO_ID: @Model.CourseId]</p>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <form asp-action="Create" method="post" class="cyber-form">
                <input type="hidden" asp-for="CourseId" />

                <div asp-validation-summary="ModelOnly" class="terminal-alert mb-4"></div>

                <div class="cyber-card mb-4">
                    <h3 class="card-header-pixel">> КОНФІГУРАЦІЯ БЛОКУ</h3>

                    <div class="mb-4">
                        <label asp-for="Title" class="pixel-label">НАЗВА МОДУЛЯ:</label>
                        <div class="input-wrapper">
                            <input asp-for="Title" class="cyber-input" placeholder="Введіть назву секції..." />
                            <span class="input-border"></span>
                        </div>
                        <span asp-validation-for="Title" class="pixel-error"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="pixel-label">ОПИС КОНТЕНТУ:</label>
                        <div class="input-wrapper">
                            <textarea asp-for="Description" class="cyber-input" rows="5" placeholder="Короткий опис того, що буде вивчатися..."></textarea>
                            <span class="input-border"></span>
                        </div>
                        <span asp-validation-for="Description" class="pixel-error"></span>
                    </div>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Details" asp-controller="AdminCourses" asp-route-id="@Model.CourseId" class="btn-cyber btn-danger-glow w-auto px-4">
                        < СКАСУВАТИ
                    </a>

                    <button type="submit" class="btn-cyber w-auto px-4">
                        [ + ІНІЦІАЛІЗУВАТИ МОДУЛЬ ]
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}